import * as react_jsx_runtime from 'react/jsx-runtime';
import React from 'react';

/**
 * IconPicker component allows users to select an icon from the provided iconsData.
 * @param {Object} props
 * @param {function} props.onSelect - Callback when an icon is selected or color is changed.
 * @param {object} [props.value] - Currently selected icon config: { name, color }.
 * @param {boolean} [props.colorPicker] - If true, enables color picking for the icon.
 */
type IconPickerSize = 'small' | 'medium' | 'large';
interface IconConfig {
    name: string;
    color?: string;
}
interface IconPickerProps {
    onSelect: (config: IconConfig) => void;
    value?: IconConfig;
    size?: IconPickerSize;
    triggerType?: 'text' | 'icon';
    triggerText?: string;
    clearable?: boolean;
    colorPicker?: boolean;
}
declare function IconPicker({ onSelect, value, size, triggerType, triggerText, clearable, colorPicker }: IconPickerProps): react_jsx_runtime.JSX.Element;

/**
 * IconRenderer renders a selected icon by name from iconsData.
 * @param {Object} props
 * @param {string} props.name - The name of the icon to render.
 * @param {string} [props.className] - Additional class names for styling.
 */
declare function IconRenderer({ name, className }: {
    name: string;
    className?: string;
}): react_jsx_runtime.JSX.Element | null;

type ColorPickerSize = "small" | "medium" | "large";
interface ColorPickerProps {
    item?: string;
    onChange?: (item: string, color: string) => void;
    data?: Record<string, string>;
    size?: ColorPickerSize;
    showValue?: boolean;
}
declare const ColorPicker: React.FC<ColorPickerProps>;

export { ColorPicker, type ColorPickerSize, IconPicker, type IconPickerSize, IconRenderer };
